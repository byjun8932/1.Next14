export default async function HomeLayout({children}) {
    return(
        <div>
            ==========홈레이아웃 시작========
            {children}
            ==========홈레이아웃 끝========
        </div>
    )
}